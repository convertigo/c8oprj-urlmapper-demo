↑default: true
comment: HSQLDB CRUD data source for UrlMapperDemo.
jdbcDriverClassName: org.hsqldb.jdbcDriver
jdbcURL: jdbc:hsqldb:file:./database/urlmapperdemo;shutdown=true
jdbcUserName: SA
↓count_resources [transactions.SqlTransaction]: 
  comment: Count Resource rows.
  sqlQuery: |
    SELECT COUNT(*) AS total
    FROM resources
↓create_resource [transactions.SqlTransaction]: 
  comment: Create one resource row.
  sqlQuery: |
    INSERT INTO resources (name, description, version)
    VALUES ({name}, {description}, {version})
  ↓name [variables.RequestableVariable-1788872183522]: 
    comment: name for resource.
    description: name for resource.
  ↓description [variables.RequestableVariable-1788872183524]: 
    comment: description for resource.
    description: description for resource.
  ↓version [variables.RequestableVariable-1788872183526]: 
    comment: version for resource.
    description: version for resource.
↓delete_resource [transactions.SqlTransaction]: 
  comment: Delete one resource row by primary key.
  sqlQuery: |
    DELETE FROM resources
    WHERE id = {id}
  ↓id [variables.RequestableVariable-1788872183538]: 
    comment: resource primary key.
    description: resource primary key.
↓init_schema [transactions.SqlTransaction]: 
  comment: Create or update the SQL schema for Resource and seed demo rows.
  sqlQuery: |
    CREATE TABLE IF NOT EXISTS resources (
      id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
      name VARCHAR(255) NOT NULL,
      description VARCHAR(255),
      version INT NOT NULL
    );
    
    DELETE FROM resources;
    
    INSERT INTO resources (id, name, description, version) VALUES
      (1, 'Guide API', 'Ressource de démonstration', 1),
      (2, 'Fichier modèle', 'Document récupérable', 1);
↓list_resources [transactions.SqlTransaction]: 
  ↑default: true
  comment: List Resource rows.
  sqlQuery: |
    SELECT base.id, base.name, base.description, base.version
    FROM resources base
    ORDER BY base.id ASC
↓read_resource [transactions.SqlTransaction]: 
  comment: Read one resource row by primary key.
  sqlQuery: |
    SELECT base.id, base.name, base.description, base.version
    FROM resources base
    WHERE base.id = {id}
  ↓id [variables.RequestableVariable-1788872183510]: 
    comment: resource primary key.
    description: resource primary key.
↓update_resource [transactions.SqlTransaction]: 
  comment: Update one resource row by primary key.
  sqlQuery: |
    UPDATE resources
    SET name = {name},
        description = {description},
        version = {version}
    WHERE id = {id}
  ↓name [variables.RequestableVariable-1788872183529]: 
    comment: name for resource.
    description: name for resource.
  ↓description [variables.RequestableVariable-1788872183531]: 
    comment: description for resource.
    description: description for resource.
  ↓version [variables.RequestableVariable-1788872183533]: 
    comment: version for resource.
    description: version for resource.
  ↓id [variables.RequestableVariable-1788872183535]: 
    comment: resource primary key.
    description: resource primary key.